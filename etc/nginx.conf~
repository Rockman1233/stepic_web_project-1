server {
	listen	80 default_server;

	root /home/box/web;

	server_name *;

	location ^~ /uploads/ {
		root /home/box/web/uploads;
	}
	location ~* ^.+\.\w+$ {
		root	/home/box/web/public;
	}
	location / {                                                   
        return 404;
	}
}
